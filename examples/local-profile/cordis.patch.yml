# Development template only. Copy this file outside the public checkout before
# running it, then replace ABSOLUTE_PATH_TO_DSH_PLUGINS with the local plugin
# repository path using forward slashes (for example <path-to-dsh-plugins>).
# The marker is intentional: this public template must not contain a machine
# specific absolute path.
- insert:
    - id: dsh-tool-example-local
      name: 'file:///ABSOLUTE_PATH_TO_DSH_PLUGINS/packages/tool-example/src/index.ts'
      config:
        prefix: 'Local'
